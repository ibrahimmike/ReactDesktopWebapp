package cityfalcon.pages.features;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class FeaturesSimilarityPage extends BasePage {
    public FeaturesSimilarityPage(WebDriver driver) {
        super(driver);
    }
}
