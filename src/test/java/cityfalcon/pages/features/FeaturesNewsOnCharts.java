package cityfalcon.pages.features;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class FeaturesNewsOnCharts extends BasePage {
    public FeaturesNewsOnCharts(WebDriver driver) {
        super(driver);
    }
}
