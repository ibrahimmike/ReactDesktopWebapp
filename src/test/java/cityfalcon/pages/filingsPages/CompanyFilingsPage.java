package cityfalcon.pages.filingsPages;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class CompanyFilingsPage extends BasePage {
    public CompanyFilingsPage(WebDriver driver) {
        super(driver);
    }
}
