package cityfalcon.pages.segments;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class SentimentAnalysis extends BasePage {

    public SentimentAnalysis(WebDriver driver) {
        super(driver);
    }
}
