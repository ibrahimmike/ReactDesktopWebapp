package cityfalcon.pages.retailPages;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class RetailTraders extends BasePage {
    public RetailTraders(WebDriver driver) {
        super(driver);
    }
}
