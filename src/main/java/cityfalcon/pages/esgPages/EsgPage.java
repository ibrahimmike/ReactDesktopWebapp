package cityfalcon.pages.esgPages;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class EsgPage extends BasePage {
    public EsgPage(WebDriver driver) {
        super(driver);
    }
}
