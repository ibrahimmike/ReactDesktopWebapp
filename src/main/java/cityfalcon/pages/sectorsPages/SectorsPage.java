package cityfalcon.pages.sectorsPages;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class SectorsPage extends BasePage {
    public SectorsPage(WebDriver driver) {
        super(driver);
    }
}
