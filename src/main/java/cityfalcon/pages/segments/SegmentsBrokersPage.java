package cityfalcon.pages.segments;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class SegmentsBrokersPage extends BasePage {

    public SegmentsBrokersPage(WebDriver driver) {
        super(driver);
    }
}
