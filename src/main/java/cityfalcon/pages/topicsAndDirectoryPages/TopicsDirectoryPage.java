package cityfalcon.pages.topicsAndDirectoryPages;

import cityfalcon.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class TopicsDirectoryPage extends BasePage {
    public TopicsDirectoryPage(WebDriver driver) {
        super(driver);
    }
}
